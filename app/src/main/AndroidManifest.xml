<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LehighStudyMate"
        tools:targetApi="31">

        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LectureSummaryActivity" />
        <activity android:name=".HomeworkTutorActivity" />
        <activity android:name=".FocusModeActivity" />
        <activity android:name=".PostureCoachActivity" />
        <activity android:name=".EmotionCareActivity" />
        <activity android:name=".SuperFocusActivity" />
        <activity android:name=".FocusResultActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".SummaryHistoryActivity" />

        <receiver android:name=".ReminderReceiver" />
        <activity android:name=".SmartPlannerActivity" />
        <activity android:name=".HabitManagerActivity" />
        <activity android:name=".FaqActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".QuizStatsActivity" />
    </application>

</manifest>